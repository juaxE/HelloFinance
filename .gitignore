# Dependencies
node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo

# Databases — the real DB must never be committable (non-negotiable #1/#5)
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3
data/

# Any real bank export kept locally for reference (README claims this is ignored;
# non-negotiable #5 — real data never enters the repo, tests, or agent context)
fixtures/private/

# Playwright
packages/web/test-results/
packages/web/playwright-report/
packages/web/blob-report/
packages/web/playwright/.cache/

# Logs / env / editor
*.log
npm-debug.log*
.env
.env.*
.DS_Store
.idea/
.vscode/
