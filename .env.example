# Copy to .env for local overrides. All optional.

# API server port (loopback only; host is hard-pinned to 127.0.0.1 and not configurable).
PORT=3001

# SQLite database file. Anything under data/ is gitignored so the real DB is never committed.
DATABASE_URL=data/app.db
